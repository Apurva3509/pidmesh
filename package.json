{
  "name": "pidmesh-dashboard",
  "private": true,
  "scripts": {
    "build:terminal": "bun build dashboard/terminal.ts --outdir dashboard/vendor --minify"
  },
  "dependencies": {
    "@xterm/addon-fit": "0.11.0",
    "@xterm/xterm": "6.0.0"
  }
}
